# 小熊貓圖鑑 — 願望池 & 路線規劃

> 想法備忘與優先序。分層依據：① 是否吃現有資料 ② 是否只動前端 ③ 是否需後端。
> 最後更新：2026-06-30

## 定位（決定優先序的依據）

**個人／創作者導向**：這個網站是作者創作體系的一部分，與作者的 **blog**（小熊貓／動物園感想、產品介紹文章）互相連結。網站負責「收集紀錄」（個體資料庫），blog 負責「書寫與產品」。

唯一倚賴他人之處：以同好在 **IG 上的公開照片**豐富個體檔案——用 **Instagram 官方 embed**（自動署名、連回原貼文）展示，尊重並回饋原作者；不直接複製圖片檔。

**資料主權**：`wiki/*.md` 是唯一正本，RPF／redpanda-lineage 只是初期建立的基礎，**非權威**。未來以作者校訂的資料為主，RPF 僅供補空白與參考；衝突時以 wiki 為準。（已寫入 CLAUDE.md）

含意：作品專區（#10）在本站只是「通往 blog 的橋」，輕量即可；IG 照片（#6）是重點；社群互動（Q&A #7、打卡 #8）與此定位較不合，降級備忘。

## ✅ 已完成

- **網站基礎**：Astro + Tailwind 靜態站，三語系（繁中／日／英），PWA（可加到主畫面、離線）
- **個體檔案 / 搜尋 / 動物園地圖**：360+ 條目，搜尋篩選、Leaflet 地圖、Google Maps 導航
- **互動家系圖**：祖先／後代展開、雙胞胎、手機可縮放拖曳
- **今天的小熊貓**（#3+#4）：首頁今日生日與今日「前往小熊星球」（🌈 溫柔語氣）
- **語言切換顯示對應名字**（#1）：中文＝漢字→英文、日文＝日文名、英文＝英文（含補齊花系漢字等）
- **動物園官網 + logo**（#2）：官網連結；logo 用官網 favicon，可在 `pipeline/data/zoo-logos.json` 手動覆蓋
- **深色模式**：以 OS 設定為優先，導覽列可手動切換並記住
- **資料完整度檢查**：`python tools/audit.py`（與 redpanda-lineage 本地比對，不重爬 RPF）

## 🔜 待做（依定位排序）

### 重點

- **資料校訂 / 更正名字**（持續）：許多條目資料（尤其名稱）需校訂，不以 RPF 羅馬拼音為準
  - 補 `chinese:` 中文名（台灣／中國個體）；修正音譯、暱稱、別名、生卒、居住史
  - 作者提供清單或逐筆指定，批次或單筆更新；預期會逐步發現並取代 RPF 初期資料的錯誤
- **IG 照片內嵌**（#6，curate 版）：wiki frontmatter 加 `instagram:` 連結 → 網站用 **Instagram 官方 embed** 顯示（自動署名、連回原貼文）
  - 用同好的公開貼文豐富個體檔案，正確標註出處；不複製圖片檔
  - 維護方式：你挑選並把 IG 連結填進對應個體
  - **現況**：整條 pipeline 已打通（`SCHEMA.md` 規範 → frontmatter `instagram:` → `build_db.py` → `export_json.py` → `Panda.astro` 官方 embed，超過 6 篇「顯示更多」、三語版權署名）。方案 C（facade + lazy-load）與帳號署名已實作（見下）。yaffa、甜甜頁已在用。

  #### 圖片集（gallery）渲染計劃 — 方案 C（✅ 已實作 2026-06-30）

  - **目標**：每隻都能有一個圖片集；先只支援 IG（X 之後再說，因 embed 機制與 IG 完全不同、且 X 近年常被登入牆擋）。
  - **核心做法（C）**：facade ＋ lazy-load（IntersectionObserver）＋「顯示更多」分批。開頁時所有照片只是輕量佔位卡（零 iframe），捲進視窗附近才換成真 embed → 直接對準效能瓶頸（iframe 數量），對「多數只有幾張」的個體零負擔、對「少數幾十張」也不爆。
  - **為何不用分頁 / 隨機重載**：兩者只是套在「每個 embed = 一個 iframe」這個昂貴核心上的外殼。分頁對多數少照片個體多餘；「隨機＋按鈕重載」每按一次拆/建 iframe，整個 session 比一次載完更耗、且內容不確定（回訪／分享看到的不一樣）。若想保留隨機探索感 → 只洗佔位卡順序（便宜），載入仍交給 lazy-load，**不要重建 iframe**。
  - **失效連結（IG 刪文／轉私密）兩層處理**：
    - 讀者層：embed 載入後以短 timeout 檢查是否填入內容，沒成功就**保守隱藏**該卡（寧可偶爾漏藏，不要誤藏好照片），讀者不會看到破版空白。
    - 作者層：靜態站伺服器端不知連結死活；用 `tools/ig_audit.py`（✅ 2026-06-30）列出/盡力檢查所有 `instagram:` 連結、標「疑似失效」與「未含帳號形式」供人工複查清理（IG oEmbed 需 token、HEAD 常被擋，不追求完美偵測；預設只列表+格式檢查，`--check` 才連網）。
  - **已知缺點與取捨**：
    - ~~圖說的「攝影者帳號」無法從 URL 自動取得~~（✅ 2026-06-30 修正）：標準 `/p/SHORTCODE/` 短連結確實不含帳號，但 IG **也支援** `/帳號/p/SHORTCODE/` 完整形式。改約定存完整形式（見 `SCHEMA.md`）後，`Panda.astro` 直接從 URL 解析發文帳號，在照片**載入前的 facade 佔位卡**顯示 `@帳號`（embed 載好後由 embed 自身頂部帳號接手，不重複）。注意：embed.js 只吃 `/p/SHORTCODE/`，故 `Panda.astro` 餵 embed 時會把含帳號的 URL 正規化掉，資料與「點照片開原貼文」仍用完整形式。
    - facade 佔位卡無法顯示真實縮圖（載入前拿不到圖）→ 用通用樣式（吉祥物＋「IG 貼文」）。
    - 自動隱藏是啟發式、跨網域讀不到 iframe 內容：慢但正常的貼文可能誤藏、IG 的「post unavailable」錯誤卡可能漏藏 → 只能「大致體驗不破」，不可靠的部分丟給作者層 audit。
    - 真 embed 載入時高度會變 → 仍有部分 layout shift，只能減輕。
    - facade 只是延後成本，使用者一互動 IG 的 cookie／追蹤／效能就回來；整個 gallery 仍 100% 依賴 IG embed.js 與其政策。
    - embed 是 client 端 iframe → 對 SEO／社群預覽（OG）零貢獻、需要 JS、alt/無障礙不可控。這些是「選 IG embed 的必然代價」，本輪接受。
  - **資料模型**：維持 `instagram:`（含選填日期後綴）不動；未來支援 X 時再加平台標記、renderer 依平台選 embed，本輪不寫。
  - **本輪實作範圍**：前端 `Panda.astro`（facade + lazy-load + 帳號署名 + embed permalink 正規化）、`SCHEMA.md` 格式約定、`tools/ig_audit.py` 失效連結稽核腳本，皆已完成。

  #### 回報／投稿與審核（規劃中 2026-06-30）

  - **回報入口維持 Tally 唯一**（資料更正 `ODr777`、圖鑑缺漏 `2EVJlb` → Google Sheet 收件匣）；不另開管道增加維護面。照片投稿若未來開放，走同一條（多加一個「IG 連結」欄位即可），不另建系統。
  - **同一隻多筆**天然支援：每次送出＝Sheet 多一列、各帶同一 slug；`instagram:` 本就是清單，多筆投稿＝同一隻多個連結，作者挑採用的寫入。唯一成本是 triage 人工。
  - **審核機制已存在且正確**：回報屬「收件匣、非權威」，沒有任何東西自動上線，由作者逐筆查證後才更新 `wiki/*.md`（人工關卡＝審核）。不需另建自動審核。可選小強化：Sheet 加「狀態」欄（new/accepted/rejected）方便自己 triage。
  - **照片眾包**：本輪**不開**（先由作者自行 curate）。
- **About／關於頁 + 與 blog 互連**（取代原 #10）：一頁「關於」介紹作者與創作體系，連到 blog；blog 也連回本站
  - 產品介紹文章住在 blog，本站只做橋
- **UI 風格調整：更鮮明活潑童趣**（#11，新增 2026-06-15）：整體視覺往更鮮明、活潑、童趣的方向調整
  - 只動前端（`web/`）；可從配色、字體、圓角、插圖／emoji、動畫等著手
  - 待定義具體風格參考與設計方向
- **資料修正機制（填表 + 生卒補資料）**（#12，新增 2026-06-15）：讓資料校訂更系統化、降低逐筆手動指定的負擔
  - 透過填表方式回報更正（名稱、生卒、居住史等）
  - 個體「死亡 / 出生」事件的標準補資料流程（死亡加 🌈、更新 `died:`；新生兒建檔並補直系親屬）
  - 注意資料主權：表單只是輸入管道，`wiki/*.md` 仍為唯一正本，由作者校訂後才寫入
- **多語系補正**（#13，新增 2026-06-15）：檢查並補齊三語系（繁中／日／英）缺漏或不一致處
  - 個體名字在各語系的對應（漢字／日文名／英文）、介面字串（`pipeline/src/i18n/`）、缺翻譯的欄位
- **資料正規化 / 定期整理**（#14，新增 2026-06-15）：評估是否需要對既有資料做正規化，並建立定期整理機制
  - 欄位格式一致性（日期 ISO、動物園名、性別、slug 消歧）、重複條目偵測、孤兒 wikilink
  - 可考慮定期跑 `tools/audit.py` 並彙整待校訂清單；仍以 wiki 為正本，整理只提示不覆蓋
- **命名由来區塊**（#15，新增 2026-06-24）：在個體頁呈現「名字的由來與命名方式」，作為跨條目的新內容類型
  - 先定義格式：放在內文哪一段（建議家族段前後新增 `## 命名由来`）、frontmatter 是否加 `naming_origin` 欄位、與既有 `nicknames` 如何分工；定好後再寫進 `SCHEMA.md`
  - 來源原則：理想上追到動物園官方說法（官網／告示）；blog（redpandapedia 等）屬轉述、非權威，採用前需查證
  - 首批對象：**陽陽、福福**（redpandapedia 有提到命名原因與方式），當格式定案後的第一個示範
  - 之後可逐步盤點哪些條目有命名故事可補

### 之後

- **他人商品 / 連結 grid**（#9）：同好商品或 IG 帳號連結
  - ⚠️ 外部商品若涉聯盟行銷需揭露；logo／商品圖有版權

### 降級／備忘（與「個人＋blog」定位較不合）

- **Q&A**（#7）：家系猜謎或 FAQ，社群互動性質，先擱置
- **打卡**（#8）：規則未定
- **IG 照片（訪客上傳版）**：需帳號與後端，與 curate 路線衝突，不採

### 技術升級（無急迫性）

- **Astro 5/6 + Tailwind 4**：等有需求再一次處理（`@tailwindcss/vite` + CSS-first 設定，需重測）
