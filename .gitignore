# 系統雜物
.DS_Store
**/.DS_Store
Untitled.canvas

# FUSE 掛載暫存檔（程式開啟中被刪/覆寫時產生，非內容）
.fuse_hidden*
**/.fuse_hidden*
_tmp_*
**/_tmp_*

# Python
__pycache__/
*.pyc

# 衍生資料庫（由 wiki 重建）
redpanda.db
test.db
test.db-journal

# 網站衍生產物（由 CI 重建）
pipeline/data/pandas.json
pipeline/data/family.json
pipeline/data/zoos.json
pipeline/data/report.json
node_modules/

# Astro（web/）
web/node_modules/
web/dist/
web/.astro/

# Obsidian 本機 UI 狀態
.obsidian/workspace.json

# Claude 本機設定
.claude/

# 機密：API 金鑰等，絕不進版控
.env
.env.*
!.env.example

# 生成圖原始檔（可由 tools/gen_images.py 重建；只 ship 優化後的 webp）
art-src/
web/public/img/*.png
# 例外：og.png 是要上站的社群分享圖（首頁 OG 卡），必須進版控才會被部署
!web/public/img/og.png

# session tooling artifacts
.agents/
skills-lock.json

# 本地工作檔（資料徵集表，不進版控）
小熊猫资料征集表.xlsx

# 待刪暫存區（雲端 session 掛載點不能刪檔，垃圾先移進來、由維護者手動清）
_to_delete/
